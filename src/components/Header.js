import NavItems from "./NavItems";
import Logo from "./Logo";

const Header = () => {
  return (
    <div className="header">
      <Logo />
      <NavItems />
    </div>
  );
};

export default Header;
